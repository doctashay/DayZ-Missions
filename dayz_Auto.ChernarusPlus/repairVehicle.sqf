
// private["_vcl"];

// _vcl = cursorTarget;
// statusChat "Repairing...";
// //Insert animation code
// sleep 30;
// statusChat "Vehicle Repaired!";
// _vcl setDamage 0;


// _damage = if (cursorTarget damage >= 1) then {
// 	isVehDamaged = true;
// };

// if (cursorTarget isKindOf 'Air') && player distance cursorTarget < 4 then {
// 	hint ("hi")
// }


// _damage = damage cursortarget;
// _distance = player distance cursortarget; 


// if (cursorTarget isKindOf 'Air' && _damage < 0.7 && _distance < 6) then { 
//  hint ("hi"); 
// }

// _damage = damage cursortarget;
// _distance = player distance cursortarget; 

// _id = addAction ["Repair Vehicle", "repairVehicle.sqf", [], 1, false, true, "", "(cursorTarget isKindOf 'Air' && _damage < 1 && _distance < 6)"];


// while true do {
// 	if (cursorTarget isKindOf 'Air' && _damage < 1 && _distance < 6) then { 
//  		hint ("hi"); 
//  		sleep 0.1;
// 	};
// };


